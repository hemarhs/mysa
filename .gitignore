# --- dependencies & build output ---
node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env
.env.local
.env*.local
.vercel
next-env.d.ts
tsconfig.tsbuildinfo

# --- secrets: never commit these ---
.env.*.local

# --- local scratch, not part of the project ---
# Transfer archives, throwaway probes and the screenshot folder the
# desktop app writes into. None of it is the project; all of it used to
# end up in commits.
*.tar.gz
diag.mjs
.writetest
Claude outputs/
